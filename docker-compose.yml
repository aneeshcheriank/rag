services:
  redis:
    image: redis:alpine
    container_name: pdr_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_pdr_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

volumes:
  redis_pdr_data: